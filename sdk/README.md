# @fusd/sdk

TypeScript client for Fusion (FUSD): PDA derivation (live), plus account decoders and
instruction builders generated from the Anchor IDL (added as on-chain flows land).

Seeds are kept in sync with `programs/fusd-core/src/constants.rs`.
